<% images_glob = Rails.application.root.join('app', 'javascript', 'application', 'images', '**', '*.{jpg,jpeg,png,svg}') %>
<% Dir.glob(images_glob).each do |image| %>
	import '<%= image %>';
<% end %>