<%= render partial: "producer_admin/events/menu_layout", locals: {
	links: {
		event: producer_admin_event_path(@event),
		event_offers: producer_admin_event_offers_path(@event),
		event_marketings: producer_admin_event_marketings_path(@event),
		event_check_in: check_in_path(@event)
	}
} %>